argument_specs:
  get_nodes:
    options:
      node_filter_names:
        type: list
        elements: str
  provision_nodes:
    options:
      node_provisioning_network_name:
        required: true
        type: str
      node_discovery_url:
        required: true
        type: str
      node_names:
        required: true
        type: list
        elements: str
      node_all_agents:
        required: true
        type: list
        elements: dict
      node_agent_namespace:
        required: true
        type: str
      wait:
        type: bool
  set_agent_metadata:
    options:
      node_names:
        required: true
        type: list
        elements: str
      node_all_agents:
        required: true
        type: list
        elements: dict
      node_agent_namespace:
        required: true
        type: str
  remove_agents:
    options:
      node_names:
        required: true
        type: list
        elements: str
      node_all_agents:
        required: true
        type: list
        elements: dict
      node_agent_namespace:
        required: true
        type: str
  clean_nodes:
    options:
      node_names:
        required: true
        type: list
        elements: str
      wait:
        type: bool
